
import requests

class AppCrawler:
	def _init_(self, starting_url, depth):
		self.starting_url = starting_url
		self.depth = depth
		self.apps = []

def crawl(self):
	return

def get_app_from_link(self, link):
	return

class App:

	def _init_(self, name, developer, price, links):
		self.name = name
		self.developer = developer
		self.price = price
		self.links = links

def _str_(self):
	return ("Name: " + self.name.enconde('UTF-8') +
		"\r\rDeveloper: " + self.developer.encode('UTF-8') +
		"\r\rPrice: " + self.price.encode('UTF-8') + "\r\n")


crawler = AppCrawler('https://itunes.apple.com/us/app/candy-crush-saga/id553834731', 0)
crawler.crawl()

for app in crawler.apps:
	print app





	
